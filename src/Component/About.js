import React from 'react';

function About(props) {
    return (
        <div>
            <p>We've given you the option to search for a brewery or brew pub by name, city or state.</p>
        </div>
    );
}

export default About;